import React from "react";

const latest = () => {
    return (
        <div>
            <h2>Hello from latest products</h2>
            <p>and the url is /products/latest</p>
        </div>
    );
};

export default latest;
